set -g mouse on
set -g history-limit 200000
set -s set-clipboard external
# Explicit SSH client terminal capabilities. Ubuntu 22.04 tmux supports
# terminal-features; the matching terminfo entries are installed in the image.
set -as terminal-features ',alacritty:RGB:clipboard'
set -as terminal-features ',xterm-kitty:RGB:clipboard'
